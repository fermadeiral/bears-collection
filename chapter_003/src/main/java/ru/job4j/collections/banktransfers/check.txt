Общее:
1. Название пакета странное - думаю, что столо бы добавить хотя бы доменное имя второго уровня.
2. Полное отсутствие javadoc-комментариев.

Account:
1. Отсутствие модификатора доступа к полям класса values и reqs (если так и задумано, то зачем тогда присутствуют геттеры). Я бы сделал их private и добавил сеттеры. reqs можно сделать final, т.к. скорей всего реквизиты счета не должны изменяться.
2. Имя поля "reqs" я бы все-таки изменил на "requisites" как в параметре конструктора. Думаю, что экономия нескольких байтов не стоит возможной путаницы.
3. Не совсем понятно употребление this в геттерах. Никакой пользы здесь не вижу.
4. В методе toString некрасивое название поля otvet, выглядит ужасно на фоне названий полей класса и переменных методов. Я бы заменил на "answer".
Так же в возвращаемой строке явно ошибка, я бы переписал строку (и метод в целом) в таком виде:
public String toString() {
    return "Account{" + "values=" + values + ", reqs='" + reqs + "'}";
}

5. Я бы еще добавил аннотации @Override к методам hashCode() и equals(Object o). 
6. В методе getReqs лишний пробел после названия метода.


Bank:
1. Много ненужных пустых строк.
2. Имя поля treemap (должно быть treeMap) не соответствует конвенции именования переменных. В данной программе я бы назвал его storage.
3. Метод delete() я бы переименовал в deleteUser, чтобы не было путаницы.
4. Метод add() я бы переименовал в addAccount(), чтобы не было путаницы.
5. Не понимаю назначения метода getActualAccount. Если он проверяет наличие определенного аккаунта у определенного пользователя, то я считаю, что проще выполнять эту проверку выражением this.treeMap.get(user).contains(account) во всех методах подобных transfer().
6. В связи с вышеизложенным, метод transfer() я бы переписал вот так (хотя если номера счетов уникальны, то всех User'ов я бы тоже убрал из метода):
	public boolean transfer(User user1, Account account1,
	                                 User user2, Account account2, double amount) {
	        return this.storage.get(user1).contains(account1)
	                && this.storage.get(user2).contains(account2)
	                && account1.transfer(account2, amount);
	    }

Convert:
1. Зачем тут пустой конструктор? ( в нем же отсутствует пробел между () и { )
2. Комментарии не в стиле javadoc.
3. В методе makeList() выражение во внутреннем цикле не заключено в фигурные скобки.
3. Лишняя пустая строка между методами, лишние пустые строки в методе makeArray().
4. В методе makeArray() выражения в if и else не заключены в фигурные скобки.

Sorter:
1. Зачем тут пустой конструктор? ( в нем же отсутствует пробел между () и { )
2. В определении метода sort параметры не должны отделяться пробелом от имени метода. Та же проблема в двух оставшихся методах и вызовах метода compare().
3. Имя метода sortnamelength не соответствует конвенции именования. Я бы назвал его sortByNameLenght.
4. Имя переменной compar выглядит не очень, ничего не мешает назвать ее comparator (то же относится к переменным compar1 и compar2).
5. Имя метода sortboth не соответствует конвенции именования. Я бы назвал его sortBoth.
6. Метод sort не совсем соответствует своему названию, т.к. он конвертирует list в TreeMap, а не сортирует.
